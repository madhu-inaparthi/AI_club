---
const eventsPlan = [
	{
		month: "Month 1",
		title: "AI Club Launch & Orientation",
		type: "Launch Event",
		description: "Grand opening ceremony with guest speakers from the AI industry",
		activities: [
			"Welcome ceremony and introductions",
			"AI industry overview presentation",
			"Club structure and goals presentation",
			"Networking session with refreshments"
		],
		duration: "3 hours",
		audience: "All students"
	},
	{
		month: "Month 2",
		title: "Prompt Engineering Workshop",
		type: "Educational Workshop",
		description: "Hands-on workshop teaching advanced prompt engineering techniques",
		activities: [
			"Interactive prompt building exercises",
			"Live demonstrations with AI models",
			"Group challenges and competitions",
			"Best practices and tips sharing"
		],
		duration: "2 hours",
		audience: "Club members + interested students"
	},
	{
		month: "Month 3",
		title: "AI Tools Showcase",
		type: "Technology Demo",
		description: "Comprehensive demonstration of cutting-edge AI tools and platforms",
		activities: [
			"Live tool demonstrations",
			"Hands-on trial sessions",
			"Comparison and evaluation workshop",
			"Q&A with industry experts"
		],
		duration: "4 hours",
		audience: "Tech enthusiasts"
	},
	{
		month: "Month 4",
		title: "AI Hackathon 2025",
		type: "Competition",
		description: "24-hour hackathon focused on solving real-world problems with AI",
		activities: [
			"Team formation and project ideation",
			"Mentorship sessions with industry professionals",
			"Development and prototyping",
			"Final presentations and judging"
		],
		duration: "24 hours",
		audience: "Students + industry professionals"
	},
	{
		month: "Month 5",
		title: "Industry Speaker Series",
		type: "Guest Lecture",
		description: "Monthly series featuring leaders from top AI companies",
		activities: [
			"Keynote presentations from industry leaders",
			"Career guidance and advice sessions",
			"Networking opportunities",
			"Panel discussions on AI trends"
		],
		duration: "2 hours",
		audience: "All university community"
	},
	{
		month: "Month 6",
		title: "Project Showcase & Graduation",
		type: "Showcase Event",
		description: "Final presentation of member projects and club achievements",
		activities: [
			"Project presentations by members",
			"Awards ceremony and recognition",
			"Alumni network introduction",
			"Celebration dinner and networking"
		],
		duration: "4 hours",
		audience: "Members, faculty, industry partners"
	}
];

const ongoingActivities = [
	{
		title: "Weekly Tech Talks",
		frequency: "Every Wednesday",
		description: "Casual presentations on latest AI developments and trends"
	},
	{
		title: "Study Groups",
		frequency: "Bi-weekly",
		description: "Collaborative learning sessions for complex topics"
	},
	{
		title: "Project Teams",
		frequency: "Ongoing",
		description: "Small groups working on semester-long AI projects"
	},
	{
		title: "Industry Visits",
		frequency: "Monthly",
		description: "Field trips to AI companies and research labs"
	}
];
---

<section id="events" class="events fade-in">
	<div class="container">
		<div class="section-header">
			<h2 class="section-title gradient-text">Events Plan</h2>
			<p class="section-subtitle">Engaging activities and experiences throughout the year</p>
		</div>
		
		<div class="events-timeline">
			{eventsPlan.map((event, index) => (
				<div class="event-card card-hover" style={`animation-delay: ${index * 0.1}s`}>
					<div class="event-header">
						<div class="event-month">{event.month}</div>
						<div class="event-type">{event.type}</div>
					</div>
					<div class="event-content">
						<h3 class="event-title">{event.title}</h3>
						<p class="event-description">{event.description}</p>
						
						<div class="event-details">
							<div class="activities-section">
								<h4>Activities:</h4>
								<ul class="activities-list">
									{event.activities.map((activity) => (
										<li>{activity}</li>
									))}
								</ul>
							</div>
							
							<div class="event-meta">
								<div class="meta-item">
									<span class="meta-icon">⏱️</span>
									<span class="meta-label">Duration: {event.duration}</span>
								</div>
								<div class="meta-item">
									<span class="meta-icon">👥</span>
									<span class="meta-label">Audience: {event.audience}</span>
								</div>
							</div>
						</div>
					</div>
					<div class="event-glow"></div>
				</div>
			))}
		</div>
		
		<div class="ongoing-activities">
			<h3 class="ongoing-title">Ongoing Activities</h3>
			<div class="activities-grid">
				{ongoingActivities.map((activity) => (
					<div class="activity-card card-hover">
						<div class="activity-header">
							<h4 class="activity-title">{activity.title}</h4>
							<div class="activity-frequency">{activity.frequency}</div>
						</div>
						<p class="activity-description">{activity.description}</p>
						<div class="activity-indicator">
							<div class="indicator-dot"></div>
							<span class="indicator-text">Active</span>
						</div>
					</div>
				))}
			</div>
		</div>
		
		<div class="event-calendar">
			<h3 class="calendar-title">Quick Calendar View</h3>
			<div class="calendar-grid">
				<div class="calendar-month">
					<h4>Jan 2025</h4>
					<div class="event-marker launch">Launch Event</div>
				</div>
				<div class="calendar-month">
					<h4>Feb 2025</h4>
					<div class="event-marker workshop">Workshop</div>
				</div>
				<div class="calendar-month">
					<h4>Mar 2025</h4>
					<div class="event-marker demo">Tech Demo</div>
				</div>
				<div class="calendar-month">
					<h4>Apr 2025</h4>
					<div class="event-marker hackathon">Hackathon</div>
				</div>
				<div class="calendar-month">
					<h4>May 2025</h4>
					<div class="event-marker speaker">Speaker Series</div>
				</div>
				<div class="calendar-month">
					<h4>Jun 2025</h4>
					<div class="event-marker showcase">Showcase</div>
				</div>
			</div>
		</div>
		
		<div class="event-registration">
			<div class="registration-content">
				<h3 class="registration-title">Ready to Join Our Events?</h3>
				<p class="registration-description">
					Be part of an exciting journey in AI innovation. Our events are designed to inspire, 
					educate, and connect you with the future of technology.
				</p>
				<div class="registration-actions">
					<a href="#home" class="glow-button">Join AI Club</a>
					<div class="contact-info">
						<span>📧 aiclub@university.edu</span>
						<span>📱 +1 (555) 123-4567</span>
					</div>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	.events {
		margin-left: 280px;
		padding: 5rem 2rem;
		background: linear-gradient(135deg, var(--bg-dark) 0%, rgba(139, 92, 246, 0.05) 50%, rgba(236, 72, 153, 0.05) 100%);
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
	}

	.section-header {
		text-align: center;
		margin-bottom: 4rem;
	}

	.section-title {
		font-size: 3rem;
		font-weight: 800;
		margin-bottom: 1rem;
	}

	.section-subtitle {
		font-size: 1.2rem;
		color: var(--text-secondary);
	}

	.events-timeline {
		display: flex;
		flex-direction: column;
		gap: 2.5rem;
		margin-bottom: 5rem;
		position: relative;
	}

	.events-timeline::before {
		content: '';
		position: absolute;
		left: 1.5rem;
		top: 0;
		bottom: 0;
		width: 2px;
		background: linear-gradient(180deg, var(--primary-blue), var(--primary-purple), var(--primary-pink));
		opacity: 0.6;
	}

	.event-card {
		background: rgba(255, 255, 255, 0.02);
		backdrop-filter: blur(20px);
		border-radius: 20px;
		padding: 2.5rem;
		border: 1px solid rgba(255, 255, 255, 0.1);
		margin-left: 4rem;
		position: relative;
		overflow: hidden;
		opacity: 0;
		transform: translateX(30px);
		animation: slideInRight 0.8s ease forwards;
	}

	.event-card::before {
		content: '';
		position: absolute;
		left: -4rem;
		top: 3rem;
		width: 1rem;
		height: 1rem;
		background: var(--primary-blue);
		border-radius: 50%;
		border: 3px solid var(--bg-dark);
		box-shadow: 0 0 15px rgba(0, 212, 255, 0.6);
	}

	.event-header {
		display: flex;
		justify-content: space-between;
		align-items: center;
		margin-bottom: 1.5rem;
		flex-wrap: wrap;
		gap: 1rem;
	}

	.event-month {
		font-size: 1.1rem;
		font-weight: 700;
		color: var(--text-accent);
		background: rgba(0, 212, 255, 0.1);
		padding: 0.5rem 1rem;
		border-radius: 8px;
		border: 1px solid rgba(0, 212, 255, 0.3);
	}

	.event-type {
		font-size: 0.9rem;
		font-weight: 600;
		padding: 0.4rem 1rem;
		border-radius: 20px;
		background: linear-gradient(45deg, var(--primary-purple), var(--primary-pink));
		color: white;
	}

	.event-title {
		font-size: 1.8rem;
		font-weight: 700;
		margin-bottom: 1rem;
		color: var(--text-primary);
	}

	.event-description {
		font-size: 1.1rem;
		color: var(--text-secondary);
		margin-bottom: 2rem;
		line-height: 1.6;
	}

	.event-details {
		display: grid;
		grid-template-columns: 2fr 1fr;
		gap: 2rem;
	}

	.activities-section h4 {
		font-size: 1.1rem;
		font-weight: 600;
		margin-bottom: 1rem;
		color: var(--text-primary);
		border-bottom: 2px solid var(--primary-purple);
		padding-bottom: 0.5rem;
	}

	.activities-list {
		list-style: none;
		padding: 0;
	}

	.activities-list li {
		padding: 0.6rem 0;
		color: var(--text-secondary);
		position: relative;
		padding-left: 1.5rem;
		border-bottom: 1px solid rgba(255, 255, 255, 0.05);
	}

	.activities-list li::before {
		content: "▸";
		position: absolute;
		left: 0;
		color: var(--primary-blue);
		font-weight: bold;
	}

	.event-meta {
		display: flex;
		flex-direction: column;
		gap: 1rem;
	}

	.meta-item {
		display: flex;
		align-items: center;
		gap: 0.8rem;
		background: rgba(255, 255, 255, 0.03);
		padding: 0.8rem;
		border-radius: 8px;
		border: 1px solid rgba(255, 255, 255, 0.1);
	}

	.meta-icon {
		font-size: 1.2rem;
	}

	.meta-label {
		font-size: 0.9rem;
		color: var(--text-secondary);
	}

	.event-glow {
		position: absolute;
		top: -50%;
		left: -50%;
		width: 200%;
		height: 200%;
		background: radial-gradient(circle, rgba(0, 212, 255, 0.05) 0%, transparent 70%);
		opacity: 0;
		transition: opacity 0.3s ease;
		pointer-events: none;
	}

	.event-card:hover .event-glow {
		opacity: 1;
	}

	.ongoing-activities {
		margin-bottom: 5rem;
	}

	.ongoing-title {
		font-size: 2.2rem;
		font-weight: 700;
		text-align: center;
		margin-bottom: 3rem;
		background: linear-gradient(45deg, var(--primary-purple), var(--primary-pink));
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
	}

	.activities-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
		gap: 2rem;
	}

	.activity-card {
		background: rgba(255, 255, 255, 0.02);
		backdrop-filter: blur(15px);
		border-radius: 16px;
		padding: 2rem;
		border: 1px solid rgba(255, 255, 255, 0.1);
		position: relative;
	}

	.activity-header {
		display: flex;
		justify-content: space-between;
		align-items: flex-start;
		margin-bottom: 1rem;
		gap: 1rem;
	}

	.activity-title {
		font-size: 1.2rem;
		font-weight: 600;
		color: var(--text-primary);
	}

	.activity-frequency {
		background: rgba(139, 92, 246, 0.2);
		color: var(--primary-purple);
		padding: 0.3rem 0.8rem;
		border-radius: 12px;
		font-size: 0.8rem;
		font-weight: 600;
		white-space: nowrap;
	}

	.activity-description {
		color: var(--text-secondary);
		margin-bottom: 1.5rem;
		line-height: 1.6;
	}

	.activity-indicator {
		display: flex;
		align-items: center;
		gap: 0.5rem;
	}

	.indicator-dot {
		width: 8px;
		height: 8px;
		background: var(--primary-blue);
		border-radius: 50%;
		box-shadow: 0 0 10px rgba(0, 212, 255, 0.6);
		animation: pulse 2s infinite;
	}

	.indicator-text {
		font-size: 0.85rem;
		color: var(--text-accent);
		font-weight: 600;
	}

	.event-calendar {
		margin-bottom: 4rem;
	}

	.calendar-title {
		font-size: 2rem;
		font-weight: 700;
		text-align: center;
		margin-bottom: 2.5rem;
		color: var(--text-primary);
	}

	.calendar-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
		gap: 1.5rem;
	}

	.calendar-month {
		background: rgba(255, 255, 255, 0.03);
		backdrop-filter: blur(10px);
		border-radius: 12px;
		padding: 1.5rem;
		text-align: center;
		border: 1px solid rgba(255, 255, 255, 0.1);
		transition: all 0.3s ease;
	}

	.calendar-month:hover {
		transform: translateY(-5px);
		border-color: rgba(0, 212, 255, 0.3);
	}

	.calendar-month h4 {
		font-size: 1.1rem;
		font-weight: 600;
		margin-bottom: 1rem;
		color: var(--text-primary);
	}

	.event-marker {
		padding: 0.5rem 0.8rem;
		border-radius: 8px;
		font-size: 0.8rem;
		font-weight: 600;
	}

	.event-marker.launch {
		background: rgba(0, 212, 255, 0.2);
		color: var(--primary-blue);
		border: 1px solid rgba(0, 212, 255, 0.3);
	}

	.event-marker.workshop {
		background: rgba(139, 92, 246, 0.2);
		color: var(--primary-purple);
		border: 1px solid rgba(139, 92, 246, 0.3);
	}

	.event-marker.demo {
		background: rgba(236, 72, 153, 0.2);
		color: var(--primary-pink);
		border: 1px solid rgba(236, 72, 153, 0.3);
	}

	.event-marker.hackathon {
		background: rgba(245, 158, 11, 0.2);
		color: #F59E0B;
		border: 1px solid rgba(245, 158, 11, 0.3);
	}

	.event-marker.speaker {
		background: rgba(34, 197, 94, 0.2);
		color: #22C55E;
		border: 1px solid rgba(34, 197, 94, 0.3);
	}

	.event-marker.showcase {
		background: rgba(168, 85, 247, 0.2);
		color: #A855F7;
		border: 1px solid rgba(168, 85, 247, 0.3);
	}

	.event-registration {
		text-align: center;
		background: linear-gradient(135deg, rgba(139, 92, 246, 0.1) 0%, rgba(236, 72, 153, 0.1) 100%);
		border-radius: 20px;
		padding: 4rem 2rem;
		border: 1px solid rgba(139, 92, 246, 0.2);
		position: relative;
		overflow: hidden;
	}

	.event-registration::before {
		content: '';
		position: absolute;
		top: -50%;
		left: -50%;
		width: 200%;
		height: 200%;
		background: radial-gradient(circle, rgba(0, 212, 255, 0.05) 0%, transparent 70%);
		animation: rotate 25s linear infinite;
	}

	.registration-content {
		position: relative;
		z-index: 2;
	}

	.registration-title {
		font-size: 2.5rem;
		font-weight: 800;
		margin-bottom: 1.5rem;
		background: linear-gradient(45deg, var(--primary-blue), var(--primary-pink));
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
	}

	.registration-description {
		font-size: 1.2rem;
		line-height: 1.7;
		color: var(--text-secondary);
		margin-bottom: 3rem;
		max-width: 600px;
		margin-left: auto;
		margin-right: auto;
	}

	.registration-actions {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 2rem;
	}

	.contact-info {
		display: flex;
		gap: 2rem;
		flex-wrap: wrap;
		justify-content: center;
	}

	.contact-info span {
		color: var(--text-secondary);
		font-size: 0.95rem;
		display: flex;
		align-items: center;
		gap: 0.5rem;
	}

	@keyframes slideInRight {
		to {
			opacity: 1;
			transform: translateX(0);
		}
	}

	@keyframes pulse {
		0%, 100% { opacity: 1; }
		50% { opacity: 0.5; }
	}

	@keyframes rotate {
		from { transform: rotate(0deg); }
		to { transform: rotate(360deg); }
	}

	@media (max-width: 768px) {
		.events {
			margin-left: 0;
			padding: 3rem 1rem;
		}
		
		.events-timeline::before {
			left: 1rem;
		}
		
		.event-card {
			margin-left: 2.5rem;
			padding: 1.5rem;
		}
		
		.event-card::before {
			left: -2.5rem;
		}
		
		.event-details {
			grid-template-columns: 1fr;
			gap: 1.5rem;
		}
		
		.event-header {
			flex-direction: column;
			align-items: flex-start;
		}
		
		.activities-grid {
			grid-template-columns: 1fr;
		}
		
		.calendar-grid {
			grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
		}
		
		.contact-info {
			flex-direction: column;
			gap: 1rem;
		}
		
		.section-title {
			font-size: 2.2rem;
		}
		
		.registration-title {
			font-size: 2rem;
		}
	}
</style>