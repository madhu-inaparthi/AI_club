---
const resources = {
	software: [
		"ChatGPT Plus subscription for advanced features",
		"Claude Pro for enhanced AI interactions",
		"Midjourney subscription for image generation",
		"GitHub Copilot for code assistance",
		"Notion AI for productivity and organization"
	],
	hardware: [
		"Laptops/computers with internet connectivity",
		"Webcams and microphones for virtual sessions",
		"Projector for presentations and workshops",
		"Whiteboard for brainstorming sessions"
	],
	online: [
		"Access to online learning platforms",
		"AI research papers and publications",
		"Industry reports and whitepapers",
		"YouTube channels and educational content",
		"Discord/Slack for community communication"
	],
	books: [
		"'Artificial Intelligence: A Guide for Thinking Humans' by Melanie Mitchell",
		"'The Hundred-Page Machine Learning Book' by Andriy Burkov",
		"'Prompt Engineering Guide' (online resource)",
		"'AI for People in a Hurry' by Neil Reddy"
	]
};

const estimatedCosts = [
	{ item: "Software Subscriptions", cost: "$200", period: "per month", priority: "High" },
	{ item: "Hardware & Equipment", cost: "$500", period: "one-time", priority: "Medium" },
	{ item: "Books & Materials", cost: "$150", period: "semester", priority: "Low" },
	{ item: "Event & Workshop Costs", cost: "$300", period: "per event", priority: "High" }
];
---

<section id="resources" class="resources fade-in">
	<div class="container">
		<div class="section-header">
			<h2 class="section-title gradient-text">Resources Needed</h2>
			<p class="section-subtitle">Essential tools and materials for our AI Club success</p>
		</div>
		
		<div class="resources-grid">
			<div class="resource-category card-hover">
				<div class="category-header">
					<div class="category-icon">💻</div>
					<h3 class="category-title">Software & Platforms</h3>
				</div>
				<ul class="resource-list">
					{resources.software.map((item) => (
						<li class="resource-item">
							<span class="item-bullet">▸</span>
							{item}
						</li>
					))}
				</ul>
			</div>
			
			<div class="resource-category card-hover">
				<div class="category-header">
					<div class="category-icon">🖥️</div>
					<h3 class="category-title">Hardware & Equipment</h3>
				</div>
				<ul class="resource-list">
					{resources.hardware.map((item) => (
						<li class="resource-item">
							<span class="item-bullet">▸</span>
							{item}
						</li>
					))}
				</ul>
			</div>
			
			<div class="resource-category card-hover">
				<div class="category-header">
					<div class="category-icon">🌐</div>
					<h3 class="category-title">Online Resources</h3>
				</div>
				<ul class="resource-list">
					{resources.online.map((item) => (
						<li class="resource-item">
							<span class="item-bullet">▸</span>
							{item}
						</li>
					))}
				</ul>
			</div>
			
			<div class="resource-category card-hover">
				<div class="category-header">
					<div class="category-icon">📚</div>
					<h3 class="category-title">Books & Learning Materials</h3>
				</div>
				<ul class="resource-list">
					{resources.books.map((item) => (
						<li class="resource-item">
							<span class="item-bullet">▸</span>
							{item}
						</li>
					))}
				</ul>
			</div>
		</div>
		
       
		
		<div class="funding-options">
			<h3 class="funding-title">Potential Funding Sources</h3>
			<div class="funding-grid">
				<div class="funding-option card-hover">
					<div class="funding-icon">🏫</div>
					<h4>University Funding</h4>
					<p>Student organization grants and department support</p>
				</div>
				<div class="funding-option card-hover">
					<div class="funding-icon">🤝</div>
					<h4>Industry Partnerships</h4>
					<p>Sponsorships from tech companies and AI startups</p>
				</div>
				<div class="funding-option card-hover">
					<div class="funding-icon">💰</div>
					<h4>Fundraising Events</h4>
					<p>Hackathons, workshops, and community events</p>
				</div>
				<div class="funding-option card-hover">
					<div class="funding-icon">👥</div>
					<h4>Member Contributions</h4>
					<p>Optional membership fees and resource sharing</p>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	.resources {
		margin-left: 280px;
		padding: 5rem 2rem;
		background: linear-gradient(135deg, var(--bg-dark) 0%, rgba(0, 212, 255, 0.03) 100%);
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
	}

	.section-header {
		text-align: center;
		margin-bottom: 4rem;
	}

	.section-title {
		font-size: 3rem;
		font-weight: 800;
		margin-bottom: 1rem;
	}

	.section-subtitle {
		font-size: 1.2rem;
		color: var(--text-secondary);
	}

	.resources-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
		gap: 2rem;
		margin-bottom: 5rem;
	}

	.resource-category {
		background: rgba(255, 255, 255, 0.02);
		backdrop-filter: blur(20px);
		border-radius: 16px;
		padding: 2rem;
		border: 1px solid rgba(255, 255, 255, 0.1);
		height: fit-content;
	}

	.category-header {
		display: flex;
		align-items: center;
		gap: 1rem;
		margin-bottom: 2rem;
		padding-bottom: 1rem;
		border-bottom: 2px solid rgba(0, 212, 255, 0.2);
	}

	.category-icon {
		font-size: 2.5rem;
		filter: drop-shadow(0 0 10px rgba(0, 212, 255, 0.3));
	}

	.category-title {
		font-size: 1.3rem;
		font-weight: 700;
		color: var(--text-primary);
	}

	.resource-list {
		list-style: none;
		padding: 0;
		margin: 0;
	}

	.resource-item {
		display: flex;
		align-items: flex-start;
		gap: 0.8rem;
		padding: 0.8rem 0;
		color: var(--text-secondary);
		line-height: 1.5;
		border-bottom: 1px solid rgba(255, 255, 255, 0.05);
		transition: all 0.3s ease;
	}

	.resource-item:hover {
		color: var(--text-primary);
		padding-left: 0.5rem;
	}

	.item-bullet {
		color: var(--primary-blue);
		font-weight: bold;
		margin-top: 0.1rem;
	}

	.cost-analysis {
		margin-bottom: 4rem;
	}

	.cost-title {
		font-size: 2.2rem;
		font-weight: 700;
		text-align: center;
		margin-bottom: 3rem;
		background: linear-gradient(45deg, var(--primary-purple), var(--primary-pink));
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
	}

	.cost-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
		gap: 1.5rem;
		margin-bottom: 3rem;
	}

	.cost-card {
		background: rgba(255, 255, 255, 0.03);
		backdrop-filter: blur(15px);
		border-radius: 12px;
		padding: 1.5rem;
		border: 1px solid rgba(255, 255, 255, 0.1);
	}

	.cost-header {
		display: flex;
		justify-content: space-between;
		align-items: flex-start;
		margin-bottom: 1rem;
	}

	.cost-item {
		font-size: 1.1rem;
		font-weight: 600;
		color: var(--text-primary);
	}

	.priority-badge {
		padding: 0.3rem 0.8rem;
		border-radius: 6px;
		font-size: 0.8rem;
		font-weight: 600;
		text-transform: uppercase;
	}

	.priority-badge.high {
		background: rgba(239, 68, 68, 0.2);
		color: #EF4444;
		border: 1px solid rgba(239, 68, 68, 0.3);
	}

	.priority-badge.medium {
		background: rgba(245, 158, 11, 0.2);
		color: #F59E0B;
		border: 1px solid rgba(245, 158, 11, 0.3);
	}

	.priority-badge.low {
		background: rgba(34, 197, 94, 0.2);
		color: #22C55E;
		border: 1px solid rgba(34, 197, 94, 0.3);
	}

	.cost-amount {
		text-align: center;
	}

	.amount {
		display: block;
		font-size: 2rem;
		font-weight: 800;
		color: var(--text-accent);
		margin-bottom: 0.3rem;
	}

	.period {
		font-size: 0.9rem;
		color: var(--text-secondary);
	}

	.cost-summary {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
		gap: 2rem;
	}

	.summary-card {
		background: linear-gradient(135deg, rgba(139, 92, 246, 0.1) 0%, rgba(236, 72, 153, 0.1) 100%);
		border-radius: 16px;
		padding: 2.5rem;
		text-align: center;
		border: 1px solid rgba(139, 92, 246, 0.2);
	}

	.summary-card h4 {
		font-size: 1.2rem;
		font-weight: 600;
		margin-bottom: 1rem;
		color: var(--text-primary);
	}

	.summary-amount {
		font-size: 2.5rem;
		font-weight: 900;
		margin-bottom: 0.5rem;
		display: block;
	}

	.summary-card p {
		color: var(--text-secondary);
		font-size: 0.95rem;
	}

	.funding-options {
		text-align: center;
	}

	.funding-title {
		font-size: 2.2rem;
		font-weight: 700;
		margin-bottom: 3rem;
		color: var(--text-primary);
	}

	.funding-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
		gap: 2rem;
	}

	.funding-option {
		background: rgba(255, 255, 255, 0.02);
		backdrop-filter: blur(15px);
		border-radius: 12px;
		padding: 2rem;
		border: 1px solid rgba(255, 255, 255, 0.1);
		text-align: center;
	}

	.funding-icon {
		font-size: 2.5rem;
		margin-bottom: 1.5rem;
		filter: drop-shadow(0 0 15px rgba(0, 212, 255, 0.3));
	}

	.funding-option h4 {
		font-size: 1.2rem;
		font-weight: 600;
		margin-bottom: 0.8rem;
		color: var(--text-primary);
	}

	.funding-option p {
		color: var(--text-secondary);
		line-height: 1.5;
		font-size: 0.95rem;
	}

	@media (max-width: 768px) {
		.resources {
			margin-left: 0;
			padding: 3rem 1rem;
		}
		
		.resources-grid {
			grid-template-columns: 1fr;
			gap: 1.5rem;
		}
		
		.cost-grid {
			grid-template-columns: 1fr;
		}
		
		.section-title {
			font-size: 2.2rem;
		}
		
		.cost-summary {
			grid-template-columns: 1fr;
		}
		
		.funding-grid {
			grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
			gap: 1.5rem;
		}
	}
</style>